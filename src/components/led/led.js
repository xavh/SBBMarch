import './led.css';

function () {
    var $winHeight = $(window).height();
    ('.container').height($winHeight);

return (
  <div class="container">
    <div class="led-box">
      <div class="led-red" ></div>
  <div class="led-green"></div> 
  <div class="led-green"></div> 
  
     
    </div>

  );
}

export default led;